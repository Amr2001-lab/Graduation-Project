# Standard Laravel/PHP ignores
/.phpunit.cache
/node_modules
/public/build
/public/hot
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json

# IDE and editor folders
/.fleet
/.idea
/.nova
/.vscode
/.zed

# --- CORRECTED RULES ---

# Ignore the *contents* of storage/app/public, but not the directory itself
/storage/app/public/*

# Now, un-ignore the specific Images folder inside it
!/storage/app/public/Images/

# ---

# Ignore the *contents* of public/storage, but not the directory itself
# This is the most common place for symlinked assets.
/public/storage/*

# Now, un-ignore the specific Images folder inside it
!/public/storage/Images/

# Keep the .gitignore file in the public/storage directory, which is a common practice
!/public/storage/.gitignore