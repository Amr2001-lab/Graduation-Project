############################
#  Standard Laravel / PHP  #
############################
/.phpunit.cache
/node_modules
/public/build
/public/hot
/storage/*.key
/storage/pail
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json

############################
#      IDE / Editor        #
############################
/.fleet
/.idea
/.nova
/.vscode
/.zed

############################################################
#  STORAGE RULES — keep Images but ignore everything else  #
############################################################

# 1) Ignore *everything* under storage/app/public …
/storage/app/public/*

# 2) …except the Images folder and its contents
!/storage/app/public/Images/
!/storage/app/public/Images/**

# 3) Always keep the placeholder .gitignore in that directory
!/storage/app/public/.gitignore

############################################################
#  PUBLIC/STORAGE RULES — same idea for the symlink target #
############################################################

# 1) Ignore everything inside public/storage …
/public/storage/*

# 2) …except its Images folder
!/public/storage/Images/
!/public/storage/Images/**

# 3) Keep the placeholder .gitignore too
!/public/storage/.gitignore
